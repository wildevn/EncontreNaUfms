// Get the information from locale tables and maybe from specific types
// depending on the type received
const listCategoryService = async (category: string) => {};

export default listCategoryService;
